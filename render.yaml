services:
  - type: web
    name: bodetronic-python
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: MASTER_USER
        value: 73221820
      - key: MASTER_PASS
        value: jdiazg20
