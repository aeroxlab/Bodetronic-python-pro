# Bodetronic Python Pro Final

Build Command:

```bash
pip install -r requirements.txt
```

Start Command:

```bash
uvicorn main:app --host 0.0.0.0 --port $PORT
```

Login maestro:

```text
Usuario: 73221820
Contraseña: jdiazg20
```
